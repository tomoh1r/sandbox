# エージェント向け作業ガイド

このファイルは、この会話で確認できる作業方針とリポジトリの現状をもとにしたガイドです。
参照できない過去の会話から、ユーザーの好みや合意事項を推測しないでください。

## コミュニケーション

- 原則として日本語で、結論を先に、簡潔に説明してください。
- 作業開始時に実施内容を伝え、時間がかかる場合は進捗や判明したことを共有してください。
- 最終報告には変更内容と確認結果を記載し、未確認事項があれば明示してください。
- コードやコマンドの識別子はそのまま使い、説明には分かりやすい言葉を選んでください。

## 作業の進め方

- 原則として、リポジトリのルートディレクトリ（この `AGENTS.md` があるディレクトリ）を
  作業ディレクトリとしてコマンドを実行してください。対象ファイルやスクリプトは、ルート
  からの相対パスで指定してください。
- README に実行例を追加・変更する場合も、基本的にルートから実行できる形にしてください。
  ツールの制約などでサブディレクトリからの実行が必要な場合は、そのコマンドだけ作業
  ディレクトリを指定し、実行場所を明記してください。
- 実行を求められた場合は、提案だけで終わらず、必要な実装と確認まで進めてください。
- 依頼の範囲内で元に戻せる通常の作業は、細かな確認を繰り返さず進めてください。
- 結果を大きく左右する不明点は確認し、それに依存しない作業を先に進めてください。
- 権限や明示的な承認が必要な場合は、対象の操作と確認が必要な理由を説明してください。
- 依頼と関係のないリファクタリング、依存関係の更新、ファイル整理は避けてください。
- サブエージェントへの委任は、明示的な依頼がある場合に行ってください。

## リポジトリの構成

- このリポジトリは、言語・ライブラリ・開発ツールを試す独立したサンプルの集合です。
- ルートの `README.md` がサンプル一覧です。
- セットアップや実行方法は各ディレクトリの `README.md` を参照してください。
- 現在のサンプルは `erlang-cluster/`、`github-actions-hello-world/`、
  `htmx-flask-quickwin/`、`javascript-marked/`、`python-pulse/` などにあります。
- `_bk/` は過去のサンプルです。古い環境向けの内容を含むため、対象として依頼された場合に扱ってください。
- リポジトリ全体に共通する言語、ビルド方法、テストコマンドがあると仮定しないでください。

## 編集と検証

- 編集前に、対象ディレクトリの説明、設定、既存コードと Git の変更状況を確認してください。
- 既存のユーザー変更を保持し、上書き、取り消し、ステージ状態の変更を勝手に行わないでください。
- ファイルや文字列の検索には、利用可能なら `rg` と `rg --files` を優先してください。
- 対象サンプルの既存の構成や書き方に合わせ、目的を満たす最小限の変更を行ってください。
- コードを追加・変更する際は、処理の目的や意図が分かる簡単な日本語コメントを添えてください。
  コメントは要点に絞り、各行の動作をそのまま説明するだけの記述は避けてください。
- 実行方法や利用者向けの動作が変わる場合は、対応する README も更新してください。サンプルの
  追加・移動時はルートの一覧も見直してください。
- 変更に応じたテストや動作確認を行ってください。文書だけの変更では、内容・リンク・差分の
  確認を基本とし、不要なテストは追加しないでください。
- 実行できなかった検証は、その理由とともに報告し、成功したものとして扱わないでください。
- コミット、プッシュ、公開などは、依頼された範囲と承認状況を確認してから実施してください。
